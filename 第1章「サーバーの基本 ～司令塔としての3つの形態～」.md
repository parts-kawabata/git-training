# 第1章「サーバーの基本　～司令塔としての3つの形態～」

## 1-1：サーバーを理解することはシステムを理解すること

**システムとサーバー**

個人として利用するシステム
- オンラインショッピングの注文システム
- 銀行やコンビニのATMシステム
- Suicaなどの交通機関のシステム

ビジネスという視点で見た場合
- 企業や団体での業務システム
- コンビニやスーパーのPOSシステム
- 工場の生産を管理するシステム
- 携帯電話の通話を管理するシステム
- 人工衛星を利用する科学技術のシステム

社会にはさまざまなシステムがあるが、一定以上の規模であれば必ず**サーバー**が存在する。

**サーバーの役割**

大半のシステムは、外見であるハードウェアとしては、サーバーと配下のコンピュータ、そしてそれらをつなぐネットワーク機器で構成されており、サーバーはその中で**中心的な役割**を果たしている。

また、中身であるソフトウェアとしては、「何がしたいか、何をさせたいか」に応じたアプリケーションソフトウェアが動いており、サーバーは**アプリケーションソフトを動作させる主役**でもある。

サーバーからシステムを見ていくことで、さまざまなシステムを理解することが容易になるとともに、やりたいことを実現するシステムがイメージできるようになる。

## 1-2：サーバーはシステムの司令塔

**司令塔としての存在**

サーバーをスポーツの世界で例えるなら「司令塔」のような存在。たくさんの選手が同時に動いているサッカーやラグビー、その他の競技では司令塔が誰であるかが必ず話題になる。状況を分析して選手に適切な指示をする、選手の疑問に答えるなど、サーバーはまさにそのような存在。

スポーツの世界と異なる部分があるとすれば、**精神的な支柱ではない**こと。あくまで技術的あるいはマネジメントの立場に徹している。

**サーバーの3つの利用形態**

サーバーには3つの利用形態がある。
- クライアントからの要求に対応して処理を実行する形態

  サーバーに接続されているクライアントPCのような配下のコンピュータからの要求に対応して受動的に処理を実行する。
- サーバーから能動的に処理を実行する形態

  サーバーが配下のコンピュータやデバイスに対して能動的に処理を実行する。
- 高い性能を活用した形態

  サーバー自体が高性能なハードウェアであることから、その特長を活かした処理を実行する。

## 1-3：クライアントからの要求に対応して処理を実行する形態

**サーバーの基本的な利用形態**

サーバーといえば**クライアント**からの要求に対応することが基本的な利用形態であり、「クラサバ」、「クライアント・サーバー」などと呼ばれるときは、そうした役割を期待されている。システムとしては配下のクライアントPCなどからの要求に応える処理で、クライアントからサーバーに要求することに始まり、サーバーは要求された処理を**受動的に実行**する。

3つの特徴
- サーバー1台に対してクライアントは複数台
- サーバーとクライアントで共通のソフトウェアを利用することが多い（サーバー用、クライアント用に分けられる場合もある）
- クライアントがサーバーに対して随時要求を上げてくる

**受動的な利用形態の代表的な例**

- ファイルサーバー
- プリントサーバー
- メールやWebのサーバー
- IoTサーバー（デバイスが随時データを上げてくる場合）

これまで一般的にサーバーと呼ばれていたものや、企業や団体における業務システムも大半は**クライアントからの要求に対応する形態に属している**。

## 1-4：サーバーから能動的に処理を実行する形態

**配下のコンピュータやデバイスに対して能動的に処理を実行する形態**

クライアントからの要求に対する処理との明確な違いは、**サーバーから処理を開始・実行する**こと。サーバーがクライアントPCや配下のコンピュータ、デバイスに対して命令する、処理を実行する形態。

3つの特徴
- サーバー1台に対してクライアントは複数台
- 必ずしもサーバーとクライアントで共通のソフトウェアを利用するわけではない
- サーバー側で処理のタイミングを定めて実行する

**能動的な利用形態の代表的な例**

- 運用監視サーバー
- RPAサーバー
- BPMSサーバー
- IoTサーバー（IoTデバイスを呼び出す場合など）

**企業や団体のシステムや業務の運営において重要な役割を果たしているサーバーである**ことが分かる。

## 1-5：高い性能を活用した形態

**高性能な処理の特徴**

サーバーはPCと異なり**高い性能を有している**。PCが普通の自動車だとすると、サーバーは用途に応じて性能や規模を変えることができるため、F1、戦車、大型トラックなどに例えることができる。これらの車両は、一般の自動車とは異なる次元の高い成果を上げることができる。

- サーバーとクライアントの構成とサーバー単体に近い構成もある
- サーバー側で独自の処理を実行する
- PCではできない高い性能が求められる

**高い性能を活用した形態の代表的な例**

- AIサーバー
- ビッグデータサーバー

AI
- 人が行っていた各種の判断や分析
- 人よりも多くの学習データを必要とする

ビッグデータ
- 多様でかつ大量のデータ
- 高速度での分析
- 構造化データと非構造化データを組み合わせて分析することもある

## 1-6：サーバーに接続する機器

**クライアントにはさまざまな種類がある**

以前からクライアント・サーバー、クラサバなどの呼び方があるように、「サーバーに接続する機器は？」と質問されたら多くの方が**クライアントPC**と答えるのは模範的な回答。

クライアントPCといっても、デスクトップPC、ノートブックPCなどさまざまにある。しかし、現在のリモート環境ということを意識すると、ノートブックPCに加えてタブレットなどもある。さらに幅を広げるとスマートフォンなどもあり得る。

**多様化するデバイス**

クライアントPCだけでなく、IoT機器などもサーバーに接続するデバイスに含めたいため、サーバーと配下のコンピュータや**デバイス**という言い方をしていた。

例えば、各種のカメラで取得した画像をサーバーで解析することもできる。ICタグ自体はデバイスというほどの機能はないが、**ICタグの中のデータをサーバーに読み込ませて処理する**ことも行われている。

つまり、現在のシステムを考えるときにサーバーに接続できるデバイスは、PCやスマートフォン以外にもさまざまなものがあり多様化しているということである。それはドローンやネットワークに接続できるロボットなどであってもよい。

## 1-7：瞬発系か持久系か？

**アプリケーションの視点**

サーバーを考えるときに何をしたいか・させたいかが重要である。

普段利用するシステムでは、次のように大きく2つに分けることができる。

- 入出力を重視するシステム

  入力したデータに対して処理結果を迅速に返すシステム。
- 集計や分析を重視するシステム

  個別に入力されたデータの集計や分析を重視するシステム。

現実にはどちらかといえばいずれかを重視するということだが、大半のシステムは両者を兼ね備えている。

**瞬発力と持久力**

入出力を重視するシステムはレスポンスが重要だから、早押しクイズのように**瞬発力**を重視するシステム。集計・分析を重視するシステムは全体のデータの入力状況を見ながら処理を進めていくため、長時間にわたる入学試験のように**持久力**が要求されるシステム。いずれにしても処理を間違うことは許容されない。

## 1-8：システムをモデル化して整理する

**モデル化の例**

例えば、サーバーがクライアントPCや各種のデバイスからデータを取得して更新するようなモデルであれば、使われるデバイスはさまざま。仮に入出力を重視するシステムだとすると、瞬発力を発揮する高速なサーバーが必要と想定することができる。また、システムとしての物理的なイメージもわいてくる。

このように軸を決めて**モデル化**できると、関係者でシステムやサーバーに関する期待や要求を具体的に確認できるとともに、求めていない機能も明確にできる。

**モデル化での留意点**

何をしたいのか、どのような処理を求めているのかについて**関係者間で共通認識を持つ**ことが重要。ここでは分かりやすさのために接続する機器の種類や数量、さらにどのようなデータをやりとりするかから入って、入出力重視と集計・分析重視の2つにまとめている。

改めて、どのように使いたいか、どのようなシステムなのかというサーバーの中身のソフトウェア的な要求と、箱であるハードウェアとしてのサーバーはどのようなものが適切かをあわせて検討していくことが必要。そのためには**中身と外見、アプリケーションソフトとハードウェアの両面から考える**必要がある。

## 1-9：基本的なシステム構成

**基本的なシステム構成の例**

最もシンプルなシステム構成は、複数台のクライアントPCとサーバー1台で、企業や団体の部門の業務システムやファイルサーバーなどの例。

サーバーを上に配置して下にクライアントPCを置いている場合、両者の間にはネットワーク機器のルータやハブがあり、LAN環境で接続されている。よくあるケースとしては企業や団体の部・課・グループごとにハブが配置されている。

例えばハブのLANポート数が24なら、24人ごとにハブが必要ということになる。実際には1台のクライアントから複数のさまざまなサーバーに接続されている。

**増えている無線LAN**

近年、個人宅でもWi-Fiを利用している方が増えているように、オフィスでの無線LANの活用も増えている。有線LANはLANケーブルの敷設工事も煩雑になってしまうが、無線LANの場合、ルータを設置すればよいだけで、LANケーブルの敷設をする必要がなく、オフィスレイアウトや座席などの自由度が高い。また、無線LANそのものの技術の向上、サーバーとクライアント、さらにソフトウェアの処理能力の向上などもある。

## 第1章のPoint

- 社会にはさまざまなシステムがあるが、一定以上の規模であれば必ずサーバーが存在する
- サーバーはシステムの中で中心的な役割を果たしている
- サーバーはシステムにおける司令塔のような存在
- サーバーには大きく3つの利用形態がある
- サーバーといえばクラサバなどの言葉のように配下のコンピュータからの要求に応える形態として捉えられることが多い
- 代表的な例として、ファイルサーバー、プリントサーバー、メールサーバーなどが挙げられる
- サーバーからクライアントへの能動的な対応は企業や団体のシステムや業務の運営において重要な役割を果たしており、今後増えていくことが想定される
- 代表的な例として、運用監視サーバー、RPAサーバー、BPMSサーバーなどが挙げられる
- サーバーの高性能を活かした形態は、今後一層拡大していく可能性が高い
- サーバーの利用形態はクラサバだけでなくさまざまな可能性がある
- サーバーに接続する機器といえばPCが代表的だが、タブレットやスマートフォンなどリモート環境を通じて多様化している
- IoTという観点では、カメラ、ICタグ、マイク、ドローン、ロボットなど、目的に応じてさまざまなデバイスがサーバーの配下に接続されている
- アプリケーションの視点では、入出力を重視するシステムか、集計・分析を重視するシステムかで考えると分かりやすい
- 前者は瞬発力が求められ後者は持久力が求められる
- サーバーの検討にあたって3つの利用形態をベースにモデル化して検討すると分かりやすい
- サーバーの中身のアプリケーションソフト、外見のハードウェアと登場人物（接続する機器）の両面で考える
- 基本的なシステム構成の例として、有線LANのネットワークであれば、サーバー、ルータ、ハブ、クライアントPCがある
- 近年は利便性からオフィスでも無線LAN接続が増えつつある
