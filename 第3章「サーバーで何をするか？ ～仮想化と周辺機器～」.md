# 第3章「サーバーで何をするか？　～仮想化と周辺機器～」

## 3-1：最初はシステム、次にサーバー

**システム化の検討**

システム化の検討に際しては最初にどのようなシステムが必要かを考える。本書ではサーバーをテーマとしているが、実は**サーバーの検討を始めるのはシステムのイメージができた後**のこと。

システムのユーザーや企画書が「このようなシステムにしたい」と頭に描いたものを具体化していく中で、どのようなサーバーが必要か検討していく。このときに役立つのが、3つの利用形態や入出力、集計・分析などのモデル化による整理。一例として、軸があることでシステム化の検討は速やかに進む。「このようなシステムにしたい」が確実にイメージできれば、どんなサーバーが必要か想定できる。

**検討するシステムの具体化**

システムに対するイメージが見えてきて、さらに利用するユーザーの人数や拠点（サイト）の数などのシステムの規模に関する具体的な数字を把握する。具体的な数字が見えてくると、**おおまかにどのようなシステムにするか説明することができる**。サーバーに関してもより具体的に見えてくる。

「このようなシステムにしたい」だけで具体的に進めるのは難しいことだが、各種の数字を加えて検討し、これまでに見てきた観点と合わせて進めることで、求められているシステムは具体的な形となっていく。

>システムの開発規模を表す2つの単位
>- 人月（にんげつ）
>  
>   1人のエンジニアが1カ月20日携わる必要があるという考え方。例えば4人で6カ月の期間が必要なシステムであれば24人月となる。
>- ステップ数
>
>   プログラム開発でプログラムの行を基準として示す考え方。システムによって異なるが、1人月が1,000～3,000ステップ前後とされている。伝統的なCOBOLなどの言語での開発で用いられる考え方で、最近はあまり使われなくなった。

## 3-2：システムの規模で構成は変わる

**システムの規模**

例えば、設立してからの年数が浅い企業で顧客管理システムを新たに導入しようということになったとき、システム化の検討を進めていく中でどのようなシステムにしたいかを明確にする。「それを実現するシステムのハードウェアやソフトウェアはどんなもの？」となるわけだが、これは**システムの規模に応じて変わる**。管理する顧客データが1,000人分か1万人分か、システムに同時にアクセスする可能性がある社員は10人か100人かなど、実際にこれほど数字が異なることはないと思うが、サーバーの選定に大きく影響を与える。

また、どれくらいの時間で顧客データの検索ができないといけないかなどの**システムの性能に関する要求**もある。システムの性能に関してはミリ秒を基準にする。超高速なシステムであれば、1,000分の数秒から1,000分の1秒を目指すこともある。外部のWebサイトを閲覧するときは多少待ち時間があっても我慢できるが、社内のサイトやシステムに関しては3秒を超えると一気に評価が下がるといわれている。

**性能見積もりとサイジング**

導入前の要件からこれくらいのサーバーの性能が必要ではないかと仮定して数値で算出することを**性能見積もり**という。性能見積もりを受けて、CPU、メモリ、ディスク、I/O性能などから、サーバーを選定することを**サイジング**という。

>**同時アクセス数（同時接続数）**
>
>あるタイミングでどれだけのユーザーからのアクセスが集中するかをいう。Webサービスやユーザー数の多い業務システムではサーバーの性能を見積もるうえで重要な数値。

## 3-3：本当にサーバーが必要か？

**PCでもかなりの仕事ができる**

以前と異なりPCは高性能になっている。機種によってはひと昔前のサーバーのスペックを有するものもある。現在ではPCでもかなりの仕事・処理ができるようになっているといっても間違いではない。特に小規模なシステムであれば再確認してほしいポイント。

どうしてもサーバーを必要とする理由
- 多数のクライアントがデータを共有するための器として使う
- システムが止まったりデータがなくなったりすることは絶対に避けたい

スタンドアロン（1台のコンピュータやソフトウェアが他のシステムやデバイスと独立して、自己完結型で機能する形態）のPCの場合には、クラサバのように**共有の器として使うことはできない**。また、PCはサーバーほど堅牢な構造にも待っていない。

>**超上流工程**：システム開発の工程において、システム設計以前の、次の工程をいう。
>- システム化の方向性
>- システム化計画
>- 要件定義

**再検討する機会を**

検討しているシステムにおいて共通の器としての機能は果たして必須なのか。例えば、多くの社員が使えるようにと考えていても、入力のタイミングやデータを調べると、入力処理は複数人で、同時には一人しか実行しないようなケースもある。不意の故障や障害に対しても、DVDやメモリなどでこまめにバックアップを取って万が一のデータの消失を防ぐという方法もある。

実はサーバーやシステムを導入する際には**投資対効果**を考えることも必要。新たに業務システムを追加導入する前に、既存のサーバーとPCにアプリケーションソフトをインストールするだけで用が足りるのではないかなどと、低コストの方法を検討する姿勢が必要。

>IT投資対効果
>
>**IT投資対効果＝効果（額）÷ 投資額**
>
>投資額は必要なコストや工数などから算出できる。効果（額）は次の事項などを組み合わせて算出する。
>- 売上増加やコスト削減などの具体的な効果数値
>- KPI（Key Performance Indicator）の達成状況
>- CS向上、ES向上、他者ベンチマークなどの価値の机上評価

## 3-4：配下のコンピュータをどのように見ているか？

**サーバーからも配下のコンピュータからも同じ**

サーバーと配下のコンピュータの間では、互いに**IPアドレス**で呼びかける。IPアドレスは**ネットワークで通信相手を識別するための番号**で、0から255までの数字を点で4つに区切って表される。

ネットワークごとに定めるから、別のネットワークに行けば同じ番号の機器も存在する可能性があり得る番号。社内のファイルサーバーなどに接続するときは、「\ \x\server01」などのようなパス名を指定し、社外のWebサイトに接続するときは、「http://www.shoeisha.co.jp」のようにURLを指定するが、私たちが意識していないだけで、それらの背後にはIPアドレスが紐づけられている。

**IPアドレスとMACアドレス**

IPアドレスは一言でいうなら、コンピュータのソフトウェアが認識するコンピュータの住所で、**MACアドレス**はハードウェアが認識する住所。MACアドレスは、自身のネットワーク内で機器を特定するための番号。実物のMACアドレスは、2桁の英数字6つを5つのコロンやハイフンでつないでいる。

アプリケーションがIPアドレスを指定し、アドレス帳をもとにMACアドレスを確認する。IPアドレスとMACアドレスの確認ができたら、目的地に向けてデータが送られ、一歩一歩着実に進んでいく。これらのステップは、ユーザーが意識することなく瞬時に行われている。

## 3-5：配下のコンピュータとのデータのやりとり

**4階層のTCP/IP**

サーバーと配下のコンピュータとのデータのやりとりには4階層で示すことができる**TCP/IPプロトコル**が使われている。サーバーと配下のコンピュータのアプリケーションソフトの間ではデータのフォーマットや送受信の手順を決めておく必要がある。例としてWebでおなじみのHTTP、メールのSMTPやPOP3などがあるが、それらはアプリケーション層のプロトコルと呼ばれている。

互いにどのようにしてデータのやりとりをするかはアプリケーション層で決まるが、続いて相手にデータを届けるのがトランスポート層の役割。トランスポート層では2つのプロトコルがある。データを送るたびに送信先とデータを明示するTCPプロトコルと、電話のように一度相手に接続したら切るまで送信先を意識することなく継続的にデータをやり取りする**UDPプロトコル**がある。

データのやりとりの決めごと、送る・届いたの次にはどのようなコースで行くか。インターネット層と呼ばれていてIPアドレスを使ってコースが決められる。

コースが決まったら最後は物理的な道具。無線のWi-Fi、Bluetooth、有線LAN、赤外線など、通信の物理的な層をネットワークインタフェース層と呼んでいる。

>アプリケーション層：HTTP、SMTP、POP3
>
>トランスポート層：TCP/UDP
>
>インターネット層：IPアドレス
>
>ネットワークインタフェース層：Wi-Fi、有線LAN、Bluetooth、赤外線

**データのカプセル化**

データをなくさないように最初のマトリョーシカに入れる→アプリケーション層のヘッダーを付加して次のマトリョーシカに入れる→トランスポート層のヘッダーを付加して次のマトリョーシカに入れる→インターネット層のヘッダーを付加して次のマトリョーシカに入れる→最後にネットワークインタフェース層のマトリョーシカに入れる

相手のネットワークに入ったら、マトリョーシカは1つずつ取られて最後にデータに戻る

## 3-6：ルータとの機能の違い

**役割の違い**

サーバは配下のコンピュータやデバイスとともに、各種のデータ処理をする。もちろん単体で高性能な処理をすることもある。それに対して**ルータ**をはじめとするネットワーク機器はさまざまなコンピュータをつなぐとともに、**データ処理が実行できるようにサポートをしてくれる**。

したがって、スタンドアロンを除けば、サーバーとネットワーク機器は一心同体で切っても切り離せない関係にある。もう少し具体的な説明をすると、TCP/IPの4階層の中で、インターネット層はコンピュータやルータなどの機器の仕事で、ネットワークインタフェース層はLANカードやハブなどが役割を果たす。

**ルータの役割**

サーバーから配下のコンピュータに対して処理を行うときに、自分と相手のコンピュータのIPアドレスとMACアドレスを指定する。ピアツーピア（ネットワーク上で機器同士が対等な関係で直接接続し、互いにデータや機能をやり取りする通信方式）通信でない限りはルータを経由する。

ルータは**送られてくるデータに対して自分が目的地に届けるのか、それとも次のルータへの中継役なのか**を常に考える。後者の場合は適切と思われる機器にデータを渡す。この繰り返しで相手のコンピュータにデータが届けられる。

サーバーはデータ処理や配下のコンピュータのマネジメントを行い、ルータはネットワークの運営において重要な役割を果たしている。なお、サーバーには**ネットワーク機器などの稼働状況を見る**機能もある。

## 3-7：サーバーの仮想化とデスクトップ仮想化

**役割の違い**

サーバーの仮想化は、物理的に1台のサーバーの中に、複数のサーバーの機能を論理的に持たせることをいう。**仮想サーバー**と呼ばれることもある。

メリット
- 2台設置するところを1台で済ませているため、設置場所、電力消費などの物理的な観点で優れている
- 仮想化できたサーバーは別のサーバーにコピーを置いたり、移動したりすることが比較的容易にでき、障害・災害対策としても有効

一方で、単純に物理サーバー1台を仮想サーバー2台に分けると**パフォーマンスが落ちる**といったデメリットもある。したがって、仮想化をする際はCPUやメモリ、ネットワーク機器などの性能を上げる必要性もある。

**デスクトップPCの仮想化**

サーバーの仮想化は進みつつあるが、クライアントPCの仮想化も進んでいる。これは**VDI**（Virtual Desktop Infrastructure）と呼ばれている。いくつかの仮想化の形態があるが、主流はサーバー側に仮想化された論理的なデスクトップPCを置いて、物理的なデスクトップであるクライアントPCはそれを表示・操作するというもの。

デスクトップPCの仮想化のしくみ
- デスクトップPCは、サーバーにいる仮想の「自分」を呼び出して仕事をする
- デスクトップPCは、仮想化された自分を呼び出せる最低限のメモリやディスクがあればよい

## 3-8：テレワーク、働き方改革の実現

**シンクライアントの普及**

**シンクライアント**（Thin Client）はハードディスクなどを搭載しない限定された性能を発揮するPCを意味する言葉で、企業や団体のセキュリティ意識の高まりとともに普及してきた。シンクライアントであればクライアントが盗難などにあっても、データが入っていないことから甚大な被害に至ることはない。

しかし、シンクライアント専用のPCは個別仕様が大半で、決して安価ではなかったことから、最近は標準仕様のPCをシンクライアントと同様な形で活用することが増えている。セキュリティソフトを活用したクライアント側のディスクやアプリケーションの利用を監視する。

したがって、シンクライアントの定義も「**処理の実行やデータの保存などをサーバーで行うクライアント**」のように変わりつつある。

**働き方改革のキー・ファクター、テレワーク**

クライアントの仮想化ができるなら、ネットワークに接続できればどこでも処理ができ、クライアントの機器自体も1つにとらわれる必要はない。外出先などで利用するノートブックPCやタブレット、自宅にある自分のPCなどから、サーバーに置かれている自分の仮想クライアントを呼び出して処理をすればよい。

いわゆる**テレワーク**（telework）やリモートワーク（remote work）。**働き方改革**を成し遂げるためには、自宅や外出先で仕事をこなすことができる生産性の高い環境が必須。そのためにはいつでもどこでも同じようなクライアント環境で仕事ができるVDIは必須。

## 3-9：ネットワークの仮想化

**ネットワーク仮想化の背景**

ネットワークの仮想化技術のひとつとして**ファブリック・ネットワーク**が注目されている。イーサネット・ファブリックなどと呼ばれることもある。

サーバーの仮想化や集積が進むと、複数のサーバーの機能が1台のサーバーに押し込められるということが繰り返し行われていく。通信環境が大きく変わらなければ、データ通信の量は以前よりはるかに大きくなり性能は減衰する。

**ファブリック・ネットワークの特徴**

サーバーの仮想化では1台のサーバーの中に複数のサーバー機能を持つこと、デスクトップPCの仮想化ではサーバーの中に複数のクライアントの機能を持つことを紹介した。ファブリック・ネットワークは複数のネットワーク機器を1台の機器のようにすることで、従来は1対1でルーティングしていたのを**マルチ対策でルーティング**する。

このようなネットワークの仮想化のアイデアはサーバーを大量に設置しているデータセンターなどから生まれているが、ここまで解説してきたさまざまな仮想化の考え方や種類を知っていると、多様なシステムに応用することができる。

1を複数に割る、自分を含めて共有の器に置く、中継地点と道を1つに見立てる、それぞれが斬新なアイデアになっている。

## 3-10：すぐに使えるサーバー

**機能ごとに専用のサーバーを設置する**

仮想化技術の活用は、サーバーをはじめとするシステムのハードウェアの導入における効率化だけでなく、働き方改革などの観点からも広がりつつある。一方、その対極に位置するのが機能ごとに専用のサーバーを設置するという発想。代表選手が**アプライアンスサーバー**。アプライアンスサーバーは特定の機能のためにセットされているサーバーで、ハードウェア、OSに加えて必要なソフトウェアがインストールされている。

したがって、**簡単な設定を済ますとすぐに使うことができる**。主にメールやインターネット関連のサーバーなどで利用されている。

**アプライアンスサーバーのメリットとデメリット**

メリット
- 簡単な設定ですぐに使える
- 必要な機能に特化しているためコストは低い

デメリット
- できることが限られ、ニーズに合わないと使えない
- 他の機能への転用は困難
- 機能ごとに台数が増えていく

なお、少し複雑になるが、**仮想アプライアンス**という考え方とともに、アプライアンスサーバーも仮想化できる。

## 3-11：サーバーのディスク

**サーバーのハードディスクの特徴**

サーバーのハードディスクは、PCに比べて性能や信頼性が高いタイプが使われている。

理由
- ユーザー数が多いので作業負荷が高い
- 24時間動作し続けるニーズがある

>ハードディスクは
>
>**迅速に応答できないといけない**
>
>（レイテンシー：通信の要求からデータが送られてくるまでの時間、ミリ秒で表示）→反応のいい人
>
>（スループット：データ転送速度　MB/秒で表示）                            →仕事の速い人
>
>かつ
>
>**たくさん**（トランザクションレート、秒での回数）**応答できないといけない** →たくさんの仕事をこなす人

**信頼性に対する要求**

サーバーの場合は、ハードディスクに障害が発生しても、**直ちに交換や増設をして業務を継続する、あるいは迅速にデータの復旧を図る**ことなどが要求される。

現在のサーバーのハードディスクは**RAID**（Redundant Array of Independent Disks）と**SAS**（Serial Attached SCSI）や**FC**（Fiber Channel）を組み合わせたタイプが多数派。PCでは**SATA**（Serial Advanced Technology Attachment）の実装が一般的。

- RAID
  
  物理的に多数並んでいるディスクを仮想的に1つに見立てて適切な位置にデータを書き込む
- SAS

  2つのポートがある。CPUと2つの道があるので性能・信頼性が高くなる。ちなみにSATAのポートは1つ。
- FCはSAS、SATAとは別格の構造で、メインフレームなどで使われる。光ファイバーなどを使用していて高価ではあるが高速転送が可能。

お皿が多数重なっているようなディスクを1つに仮想的に見立てるRAIDとサーバーとのインタフェースが複数あるSASは障害に強いため信頼性が高い。ハードウェアやソフトウェアを語るときに仮想化という言葉はなくてはならない言葉だが、仮想化の歴史の中では実はハードディスクが最初といわれている。技術動向としてはSSD（Solid State Drive）と呼ばれているフラッシュメモリをベースとしたタイプのディスクの普及・拡大も期待されている。

## 第3章のPoint
- システム化の検討においては最初にサーバを考えることはない
- 最初にシステムのイメージを描いてからサーバーが検討される
- システムの規模に応じて選定されるサーバーは変わる
- 適切なサーバーを選定することをサイジングと呼び、その前に性能見積もりが必要
- 小規模なシステムの場合には、PCでも実現可能ではないかと確認する考え方も必要
- 投資対効果も意識すること
- コンピュータ同士はデータのやりとりをする相手をIPアドレスとMACアドレスで特定している
- IPアドレスはソフトウェアが認識するコンピュータの住所で、MACアドレスはハードウェアの住所にあたる
- サーバーと配下のコンピュータのデータのやりとりはTCP/IPプロトコルが使われていて階層構造となっている
- 各層でカプセル化されて別の階層に進んでいく
- ルータはネットワークの中で重要な役割を果たしている
- サーバーはネットワークやネットワーク機器の稼働状況を見ることもできる
- サーバーの仮想化は1台のサーバーに論理的に複数のサーバーの機能を持たせることで、コスト削減、サーバーの集約、障害対策としても有効
- 一方で仮想化されるとレスポンス性能が落ちることもある
- クライアントPC（デスクトップ）の仮想化も進んでいる
- 時代とともにシンクライアントの意味が変わりつつある
- 働き方改革を支えるテレワーク環境の実現にはVDIの活用が必須
- サーバーの仮想化や集約化に従って、ネットワークの負荷が増大し、ネットワークの仮想化も必要となった
- ファブリック・ネットワークはサーバーの集約化とともに広がっていく可能性が高い
- 比較的すぐに使えるサーバーとしてアプライアンスサーバーがあり、特定の機能に対して必要なソフトウェアがインストールされている
- 仮想アプライアンスという考え方があり、アプライアンスサーバーも仮想化できる
- サーバーのハードディスクはPCに比べて性能と信頼性が高いものが実装されている
- 現在はRAIDとSASが一般的
